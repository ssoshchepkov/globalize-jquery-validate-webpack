declare module "globalize-config-compiler" {

    export = globalize_config_compiler;

    function globalize_config_compiler(config: any, options?: any): {
        [x: string]: string;
    };

    namespace globalize_config_compiler {
        const prototype: {
        };

    }
}